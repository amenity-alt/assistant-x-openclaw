# 核心依赖
numpy>=1.21.0
librosa>=0.9.0
sounddevice>=0.4.0
soundfile>=0.12.0
sherpa-onnx>=1.9.0

# TTS
# 注意: 包名是 piper-tts，安装后 import 时用 from piper import PiperVoice
# 切勿 pip install piper（那是另一个无关包 pypiper）
emoji>=2.0.0
piper-tts>=1.0.0
onnxruntime>=1.20.0
# 金属感语音后处理用：自带跨平台静态 ffmpeg 二进制，免系统安装/PATH
imageio-ffmpeg>=0.4.9

# OpenClaw 桥接
requests>=2.28.0
websocket-client>=1.6.0   # WebSocket 桥 (import websocket)
cryptography>=41.0.0      # WebSocket 桥设备签名 (Ed25519)

# Vision Mode 手部跟踪（Phase 2；模型 models/hand_landmarker.task 首次使用自动下载）
mediapipe==0.10.35
